Modify the SaneApe.com web app (using Flask backend, HTML/JS frontend) to incorporate a specialized analysis for income-generating ETFs like YieldMax products (e.g., MSTY, ULTY, PLTY, TSLY, NVDY, CONY, etc.). The goal is to add an optional "Income-Focused Analysis" mode that uses a custom method to evaluate these for income seekers, without breaking existing functionality (standard technical analysis with 35 indicators via OpenAI). The best integration is to add a checkbox in the input form: "Analyze primarily for income generation?" This is user-friendly, non-intrusive (no popup), and allows personalization, as per best practices in financial apps like customizable dashboards in trading platforms (e.g., avoiding flow interruptions while enabling mode switches).

If the checkbox is checked (or optionally auto-detect if ticker matches a predefined list for YieldMax ETFs), compute income-specific metrics and include them in the OpenAI prompt for a tailored recommendation. Always run the standard technical analysis, but add an "Income Analysis" section in results if applicable. This keeps the app modular and ensures income seekers get differentiated insights (focusing on yield vs. erosion, unlike pure technicals).

Detailed method to implement (embed this logic):

Detection: Maintain a list in code: yield_etfs = ['MSTY', 'ULTY', 'PLTY', 'TSLY', 'OARK', 'APLY', 'NVDY', 'AMZY', 'FBY', 'GOOY', 'NFLY', 'CONY', 'DISO', 'XOMO', 'MRNY', 'AIYY', 'SQYY', 'YMAX', 'AMDY', 'PYPY', 'JPMO', 'MCDO', 'MAXI', 'YBIT', 'CRSY', 'JPMY', 'HOOY', 'CVNY'] # Update as needed.
If user checks box or ticker.upper() in yield_etfs, flag income_mode = True.
Compute metrics using yfinance (after fetching df = yf.download(ticker, period='2y')) and ticker_info = yf.Ticker(ticker).info:
Annualized Distribution Yield: Use ticker_info.get('trailingAnnualDividendYield', 0) * 100 or calculate from df['Dividends'].sum() / len(df) * 12 / df['Close'].iloc[-1] * 100 (approximate monthly).
Annualized NAV Decay Rate: If len(df) > 1: ((df['Close'].iloc[0] - df['Close'].iloc[-1]) / df['Close'].iloc[0]) / (len(df)/365) * 100, else 0.
Expense Ratio: ticker_info.get('expenseRatio', 0.99) * 100 # Default to 0.99% for YieldMax.
Estimated Tax Drag: Assume 25% (adjustable; for ordinary income/ROC).
Effective Income Return: yield - decay - expense - tax_drag.
ROC Percentage: Hardcode ~95% for YieldMax or fetch if possible (e.g., from summaries).
Total Return with Reinvestment: df['Cum_Div'] = df['Dividends'].cumsum(); total_return = ((df['Close'].iloc[-1] + df['Cum_Div'].iloc[-1]) / df['Close'].iloc[0] - 1) * 100.
Buy if effective_return > 5 (threshold; configurable).
Serialize metrics to JSON: income_metrics = {'yield': ..., 'decay': ..., 'effective_return': ..., ...}
UI/Frontend Changes (HTML/JS):

In the form, add:  Analyze primarily for income generation? # After ticker input.
On submit, include in POST data: e.g., fetch with FormData including checked state.
In results display: If response.income_analysis exists, add a
with table of metrics, buy/no-buy badge (e.g., green "Buy for Income" if >5%, red "No Buy"), and explanation. Use collapsible accordion for details.
Backend Changes (Python/Flask):

In route handler: income_focus = request.form.get('income_focus') == 'on'
If income_focus or ticker.upper() in yield_etfs: Compute metrics, add to OpenAI prompt.
Update OpenAI prompt (embed revised version): "You are an expert stock technical analyst. [Existing technical prompt here]... Additionally, if this is an income-generating ETF, perform income-focused analysis using these metrics: [INSERT JSON income_metrics]. Calculate Effective Income Return = Annualized Distribution Yield - Annualized NAV Decay Rate - Expense Ratio - Estimated Tax Drag. Recommend 'Buy for Income' if >5%, else 'No Buy'. Explain risks (e.g., NAV erosion, ROC tax implications, volatility). Include in JSON output: {'income_recommendation': 'Buy for Income' or 'No Buy', 'effective_return': value, 'income_explanation': 'brief', ...} alongside standard fields."
Parse response to extract income fields, add to JSON returned to frontend.
Error Handling: If data unavailable (e.g., new ETF), fallback to "Insufficient data for income analysis."
Test: Ensure standard flow works without checkbox; income mode adds section without errors.
Output modified code snippets only (HTML form, JS submit, Python route/OpenAI call).